
# Dotfiles


This repository contains my current dotfiles. 


## Usage

Make sure you have the appropriate packages installed, for example:

```bash
  sudo apt install polybar picom
```

Clone the repository and copy the desired folders to *~/.config/*
